This folder contains the environment requirements for the project, which was built using Python 3.11. 

To install the dependencies, run the following command:

```bash
pip install -r requirements.txt
```
