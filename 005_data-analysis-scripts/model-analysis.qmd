---
title: "model-visualization"
format: html
editor: visual
---

## Quarto

```{r}
library(tidyverse)
library(brms)
library(ggdist)
library(tidybayes)
```


```{r}
model = readRDS("../006_models/dialysis-model_13.rds")

```

You can add options to executable code like this

```{r}

```

The `echo: false` option disables the printing of code (only output is displayed).
